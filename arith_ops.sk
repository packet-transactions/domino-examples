generator int abc_arith(int a, int b, int c) {
  bit a_select = ??(1);
  bit b_select = ??(1);
  bit c_select = ??(1);

  a = a_select ? a : 0;
  b = b_select ? b : 0;
  c = c_select ? c : 0;

  bit op_select = ??(1);
  return op_select ? a + b - c : a + c - b;
}
